import Link from "next/link";
import { ArrowLeft, ArrowRight, CalendarDays } from "lucide-react";
import { PublicDocsShell } from "@/components/public-docs-shell";
export default async function BlogArticlePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const title = slug === "memory-with-boundaries" ? "Memory with boundaries: scopes, Spaces, and retrieval" : slug === "from-rest-to-mcp" ? "From REST to MCP: one memory contract, many clients" : "Building the memory layer for universal AI applications"; return <PublicDocsShell><main><article><header className="mx-auto max-w-[880px] px-5 pb-14 pt-16 sm:px-8 lg:pt-24"><Link href="/blog" className="inline-flex items-center gap-2 text-xs font-semibold text-[#64748b] hover:text-[#1769e0]"><ArrowLeft className="size-3" /> All notes</Link><p className="mt-16 text-[10px] font-semibold uppercase tracking-[.18em] text-[#1769e0]">BUILDING · AUGUST 2026</p><h1 className="mt-5 text-5xl font-semibold leading-[.98] tracking-[-.07em] sm:text-7xl">{title}</h1><div className="mt-7 flex items-center gap-2 text-xs text-[#64748b]"><CalendarDays className="size-4" /> TrueMemory team · 6 min read</div></header><div className="border-y border-[#dbe3ee] bg-[linear-gradient(135deg,#eff6ff,#ffffff)]"><div className="mx-auto h-72 max-w-[880px] px-5 sm:px-8" /></div><div className="mx-auto grid max-w-[1120px] gap-10 px-5 py-14 sm:px-8 lg:grid-cols-[170px_680px]"><aside className="text-[10px] font-semibold uppercase tracking-[.16em] text-[#94a3b8]">Share this note<br /><div className="mt-4 flex gap-2"><span className="grid size-8 place-items-center border border-[#dbe3ee]">X</span><span className="grid size-8 place-items-center border border-[#dbe3ee]">↗</span></div></aside><div className="prose prose-slate max-w-none"><p className="text-xl leading-9 text-[#334155]">Memory is one of the hardest parts of building reliable AI systems. The model can reason, but it needs the right context at the right time—and that context needs a boundary.</p><p>TrueMemory is built around a simple idea: memory should be infrastructure. Applications and agents can write, search, and retrieve through one contract while authorization, workspace scope, provenance, and retrieval remain explicit.</p><h2>One layer, many clients</h2><p>The platform exposes REST and MCP interfaces, with SDK-friendly HTTP paths. That means a product does not need to couple its user experience to the storage or retrieval implementation underneath.</p><div className="my-8 border border-[#bfdbfe] bg-[#eff6ff] p-5 text-sm leading-7 text-[#334155]"><strong className="text-[#1769e0]">The memory loop</strong><br />Remember durable context → search for relevant context → retrieve authorized memories → revise or forget when the source changes.</div><h2>Boundaries are part of the product</h2><p>Tokens can carry memory scope and bindings to a Space or agent. Those bindings are checked at the API boundary so a client cannot widen its own access through a request parameter.</p><p className="border-t border-[#dbe3ee] pt-8 text-sm text-[#64748b]">This note describes the current repository-backed implementation. Future ideas are intentionally kept separate from verified behavior.</p><Link href="/docs" className="mt-6 inline-flex items-center text-sm font-semibold text-[#1769e0]">Continue in the docs <ArrowRight className="ml-2 size-4" /></Link></div></div></article></main></PublicDocsShell>; }
