# TrueMemory Phase 11.5 — Performance Results

No latency percentiles are reported without a real isolated provider/browser run. TTFT, total response, memory lookup, tool rounds, memory write, catalog load, and usage analytics latency remain **NOT VERIFIED**.

The Windows Python cache workaround is verified: focused tests run with a writable `PYTHONPYCACHEPREFIX` and do not modify application behavior.
