# Memory Fabric

The Memory Fabric is the authorized shared substrate across agents and deployments: REST, MCP and SDKs converge on the same scoped MemoryCore contract. Agent identity is provenance by default, not ownership. Sharing requires authorization and revocation semantics.
