# TrueMemory Phase 11.4 — Browser E2E

Status: not run against a live authenticated browser session in this environment.

Required scenarios remain:

- model picker search, scroll, badges, and selection persistence;
- OpenAI/OpenRouter provider switching;
- workspace switching and recent/pinned chat reload;
- usage popover and `/usage` analytics;
- cross-provider memory continuity.

The current frontend type-check passes. Live browser evidence must be collected against the deployed Vercel/Render environment with a test account and must not use production destructive operations.
