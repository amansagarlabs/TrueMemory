# Phase 10.3E Portability

Portable Memory v1 remains the loss-minimizing machine format. Memory Notes export now provides human-readable semantic sharing through `POST /v1/memory/export/notes`; it does not preserve memory ids, exact revisions, or full provenance. Isolated deployment and restart durability remain unverified.
