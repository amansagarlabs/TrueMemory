[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "truememory-memory"
version = "0.1.0"
description = "Thin Python client for the TrueMemory Memory API"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
