# Optional high-fidelity document OCR. Keep this in a separate virtual environment
# for production, as recommended by PaddleOCR's own installation guide.
-r requirements.txt
paddleocr[doc-parser]>=3.6.0,<4.0.0

# Install exactly one matching inference engine separately. CPU example:
# python -m pip install paddlepaddle==3.2.1 \
#   -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
